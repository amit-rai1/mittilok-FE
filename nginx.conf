server {
    listen 80;
    server_name mittilok.in www.mittilok.in;

    root /var/www/mittilok-FE/dist;
    index index.html;

    # SPA fallback — required for BrowserRouter paths like /nursery, /login
    location / {
        try_files $uri $uri/ /index.html;
    }
}
